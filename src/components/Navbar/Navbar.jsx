import { Navbar, Container } from 'react-bootstrap'

const NavbarApp = () => {

    return (
        <Navbar bg="dark" variant="dark">
            <Container>
                <Navbar.Brand>
                    React-useContext
                </Navbar.Brand>
            </Container>
        </Navbar>
    )
}

export default NavbarApp